stages:
  - build

image: node:latest

before_script:
  - yarn

cache:
  paths:
    - node_modules/

build:
  stage: build
  script:
    - yarn build
    - echo '//registry.npmjs.org/:_authToken=${NPM_TOKEN}'>.npmrc
    - yarn publish
  only:
    - tags

